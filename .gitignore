# dependencies (bun install)
node_modules

# output
out
# ...except the committed solc artifact: contracts/out/DuelEscrow.json is the
# reviewed, deployed bytecode and contracts/deploy.ts refuses to run without it.
!contracts/out/
dist
*.tgz

# code coverage
coverage
*.lcov

# logs
logs
_.log
report.[0-9]_.[0-9]_.[0-9]_.[0-9]_.json

# dotenv environment variable files
.env
.env.development.local
.env.test.local
.env.production.local
.env.local

# caches
.eslintcache
.cache
*.tsbuildinfo

# IntelliJ based IDEs
.idea

# Finder (MacOS) folder config
.DS_Store

# operator-supplied audio: licensed tracks stay out of the repo
*.mp3
src/assets/*.mp3
